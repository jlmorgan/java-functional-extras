# `Maybe.<V>nothing()`

Creates a nothing to represent `null` or a missing value.

## Types

* `V`: The underlying type.

## Returns

* `(Maybe<V>)`: A `Nothing`.

## Example

```java
Maybe.<String>nothing();
// => Nothing()
```
