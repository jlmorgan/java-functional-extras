# `id(a)`

The `I` combinator or identity morphism.

## Arguments

* `a (A)`: The input value.

### Types

* `A`: Type of the argument.

## Returns

* `(<A>)`: The input value.

## Example

```java
Date value = new Date();

id(value) == value;
// => true
```
